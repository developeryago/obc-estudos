function victory (club:{name: string, position?: number})  {
    // acrescenta três pontos ao clube 
}

victory({name: "Liverpool", position: 1})
victory({name: "Fluzão"})

function verification(test) {
    if (test) {

    } else {
        let _check: never
        let text = _check
        text = ""
    }
}